\clearpage
\section{Conclusions and Future Work}

\subsection{Conclusions}

This thesis addresses the implementation of a flexible open source EDA 
tool for exploring the large and
strongly coupled design space for implementing application-specific
DNN accelerators. Architectural
parameters such as the processing-element array, memory hierarchy and
mapping determine the behavior of a design, but its final power,
performance and area also depend on the physical blocks available in a
particular technology. Exploring all these decisions simultaneously at
implementation level is generally impractical, while considering only
an abstract architecture can lead to candidates that cannot be
implemented efficiently.

The main contribution of this work is Talos, a hierarchical design
space exploration framework that separates these decisions into two
connected levels. Level~1 explores abstract accelerator architectures
using a discrete genome, NSGA-II, and ZigZag. Selected candidates are
converted into an abstract accelerator representation that records
their compute and memory requirements. Level~2 then matches these
requirements with characterized processing elements and memory IPs and
evaluates the resulting implementations using workload-aware energy,
power, performance, area and timing metrics. This intermediate
representation is the key connection between both levels: it preserves
the architectural decision and its mapping while allowing several
physical implementations to be considered.

The implemented framework also separates raw evaluation metrics from
optimization objectives. Consequently, the same evaluation flow can
be used to optimize energy, area or performance individually or in
combination, without embedding one fixed design criterion in the
evaluator. NSGA-II is available for large exploration spaces, whereas
exhaustive enumeration covers all compatible IP combinations when the
remaining Level~2 space is small. Feasibility checks ensure that the
selected IPs provide the required capacity, bandwidth, throughput and
operating frequency.

The milestones established in Subsection~\ref{subsec:milestones} were
therefore fulfilled within the scope of the project.
Milestones~\ref{milestone:m1} and~\ref{milestone:m2} are justified in
the Methodology chapter: a hierarchical two-level exploration
methodology was defined and implemented, and candidate accelerator
architectures can be automatically generated and evaluated through
ZigZag. Milestones~\ref{milestone:m3} and~\ref{milestone:m4} are
justified in the Project Development chapter: technology-dependent
hardware is represented through characterized IP pools, and the
evaluation and optimization interfaces remain modular and extensible.
Finally, Milestone~\ref{milestone:m5} is covered by the Results
chapter, where the complete flow was validated using representative
workloads, published accelerator references and synthesized
implementations.

The complete INT16 AlexNet experiment demonstrates the practical
effect of the optimization objectives. Energy-only, area-only and
performance-only exploration produced markedly different designs, and
optimizing performance without an area or energy objective resulted in
an impractically large accelerator. In contrast, the combined E+A+P
objective selected a balanced implementation. More generally, the
experiments confirm that Talos exposes the trade-offs between
objectives instead of hiding them behind a fixed accelerator template
or returning a single optimum without context.

The comparisons with Eyeriss v1, Eyeriss v2 and SAURIA provide a more
demanding reference for the quality of the selected designs. Talos can
identify configurations with competitive area or performance for some
workloads, although custom accelerators remain more efficient in the
metrics and operating conditions for which they were specifically
designed. These comparisons should therefore not be interpreted as
chip-to-chip superiority claims. Instead, they show that the framework
can identify meaningful architectural alternatives and make their
trade-offs and limitations explicit.

The results also demonstrate the importance of defining the system
boundary correctly. In several configurations, modeled external DRAM
energy is much larger than the energy consumed by the on-chip
accelerator. Reporting on-chip and total values separately avoids
attributing external-memory costs to the accelerator and prevents
small architectures with excessive off-chip traffic from appearing
artificially efficient.

Synthesis of the selected E+A+P configurations provides an independent
check of the Level~2 estimates. Across the INT8, INT16, and FP16
configurations, Talos underestimates synthesized area by 1.18--3.63\%
and conservatively overestimates active power by 10.73--23.12\%. This
agreement supports using the model to reduce and rank the design space,
but it does not replace synthesis or physical implementation of the
final candidates.

From a practical point of view, a complete Talos exploration takes
from a few hours to a few days, depending on the workload, the
population size and the number of generations. This is short compared
with the time required to implement and evaluate even a single
accelerator candidate by hand, which is the process that the tool is
meant to accelerate. The framework is open source~\cite{talos_repo} and
modular, so its
workloads, IP pools, constraints and objectives can be changed without
modifying the exploration flow, and new evaluation models can be
added on top of the raw metrics.

Within this scope, the results are consistent with the hypothesis of
this thesis: separating technology-independent architectural
exploration from technology-dependent IP instantiation makes the
design of DNN accelerators more manageable, while the modular
evaluation interfaces allow custom estimations at both levels. Each
part of this claim rests on a different set of results. The synthesis
validation shows that the Level~2 estimates obtained after this
separation remain close to implementation values; the AlexNet
objective study shows that the modular objectives expose real
trade-offs instead of hiding them; and the reference comparisons show
that the selected designs are meaningful architectural alternatives
rather than artifacts of the model. The hierarchical organization
itself provides the manageability: technology-dependent IP evaluation
is restricted to architectures retained after Level~1, and
conventional synthesis is required only for the final candidate set
rather than for the complete architectural design space.

Talos should therefore be understood as a decision-support and 
design-space reduction tool: it produces
traceable feasible alternatives together with their generated
mappings and exposes their trade-offs, after which a small set of
candidates can be passed to a conventional hardware implementation
flow.

%\subsection{Limitations}
%The current version of Talos should be understood as a working
%prototype of an architectural exploration tool. Some parts are
%intentionally simplified and define how its results should be
%interpreted.
%
%First, the Level~1 area model is a pool-dependent lower bound. It is
%built from the minimum-area compatible IPs of the pool, so it is
%useful to rank architectures, but it is not a technology-accurate
%area model, and it requires the pool to contain compatible
%implementations for the whole architectural space.
%
%Second, the Level~1 design space is defined through a fixed catalog
%of discrete values. This keeps candidate generation valid and
%manageable, but it also makes the exploration rigid and may leave
%relevant architectural values outside the search.
%
%Third, the Level~2 evaluator does not model pipeline effects,
%interconnect and clock-tree cost, memory arbitration, control logic,
%DRAM idle power or the physical memory interface. The synthesis
%validation shows that area is still well estimated for the evaluated
%configurations, but active power is overestimated by 10--23\%, and
%both figures are only valid within the components and operating
%conditions covered by the characterization.
%
%Finally, the IP pool used in this thesis was built manually for a
%single technology and operating point. Talos does not scale the
%characterized values to other voltages or frequencies, so any other
%target requires a new characterization.

\subsection{Future Work and limitations}

The most immediate continuation of this work is to address the
limitations of the tool, starting with the fidelity and calibration
of the Level~2 model. Interconnect, control,
pipelining and clock-tree overheads could be added using synthesis data
from a larger set of generated configurations. In addition, defining a
benchmarking protocol against existing exploration frameworks, covering
exploration runtime, memory usage and the quality of the resulting
solution sets over a shared portion of the design space, would allow
the efficiency of the tool itself to be assessed quantitatively.

A second direction is to expand and automate hardware
characterization. Larger IP pools covering more precisions,
technologies, voltage and frequency operating points would allow Talos
to explore a broader implementation space. An automated flow from RTL
synthesis to validated IP-pool entries would reduce manual effort and
make characterization conditions and provenance reproducible.

The interaction between the two exploration levels can also be made
more flexible. Level~2 could request a new mapping when a physical
implementation cannot sustain the Level~1 activity profile, or feed
technology-dependent information back into a new Level~1 iteration.
This would preserve the computational advantage of hierarchical
exploration while recovering candidates that are rejected only because
the original mapping is too demanding for the selected IPs.

External memory should be represented in greater detail. Future models
could include memory-controller and interface area, power and bandwidth
constraints, and support several DRAM technologies or platform
configurations. This would make it possible to explore on-chip storage
and external bandwidth jointly without treating additional bandwidth
as a cost-free resource.

Finally, Talos could be connected to both AI-based EDA tools and/or an RTL composition and physical
implementation flow. Generating a structural design from the selected
IP configuration, synthesizing it automatically and returning the
measured results to the characterization database would close the loop
between exploration and implementation. This extension would turn the
current decision-support framework into an end-to-end system while
retaining synthesis as the final source of physical accuracy.
