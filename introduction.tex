\clearpage
\newpage
\section{Introduction}
\subsection{Motivation}

%{An Introduction that clearly states the rationale of the thesis
%that includes:}

The computational demands of data-intensive workloads have driven
the advances in microarchitecture, particularly toward highly
parallel designs that leverage SIMD execution and specialized 
tensor-processing units with computational efficiency and
throughput. Such improvements turns the promotion of AI a
reality in human daily lives. Deep Neural
Networks have become a very common solution to complex tasks such as
computer vision, speech recognition or natural language processing.
The recent breakthroughs of AI results in a raising demand of
accelerators. However, the increasing accuracy of latest DNN models
comes at the cost of higher computational and memory requirements.

Executing these tasks efficiently is a challenge in itself. A general
purpose accelerator such as a CPU or a GPU is often inefficient, and
can become critical in certain scenarios such as edge computing or
embedded systems, where power consumption or area are a big
constraint. As a result, a big portion of the market has focused in
the development of domain-specific hardware accelerators, tailored to
certain DNNs workloads or models.

Different accelerators have approached this problem in very different
ways. Eyeriss uses a spatial architecture focused on data reuse
\cite{eyeriss}, while Google's TPU uses a systolic array designed for
high-throughput matrix operations \cite{tpuv1}. Other designs explore
different parts of the design space. Envision introduces
precision-scalable computation \cite{envision}, TinyVers targets
ultra-low-power edge inference \cite{tinyvers}, and DIANA combines
digital acceleration with analog in-memory computing \cite{diana}.
These examples show that there is no single accelerator architecture
that fits every workload or target platform.

\subsection{Challenges in Accelerator Design}
Designing an accelerator is no easy feat. These devices can be
defined by numerous design parameters: size, topology of the
processing elements, memory hierarchy, algorithm to hardware mapping,
pipeline structure, etc.
The vast number of variables results in a vast design space, making
manual exploration impractical.

\subsection{Design Space Exploration Frameworks}
To address this challenge, several Design Space Exploration (DSE)
frameworks have been
proposed. Tools such as DNN-Chip Predictor \cite{chippredictor} and
ZigZag \cite{zigzag} tackle this issue by enabling a fast exploration
of the design space.
These tools allow designers to evaluate a large number of
architectures without performing
full hardware implementation or evaluations manually.

More recent works broaden the scope of this exploration. For edge
systems, Rocha et al. propose a system-technology co-optimization
framework that connects workload, architecture, mapping, physical
design, 3-D integration, and nonvolatile-memory decisions
\cite{rocha2024stco}. Accelet extends hardware--software co-design to
chiplet-based DNN accelerators by exploring functional units,
interconnect topologies, system architectures, and dataflow scheduling
and mapping \cite{zheng2026accelet}.

Other frameworks use different exploration strategies. ConfuciuX
\cite{confuciux}, for example, uses reinforcement learning to search
for suitable compute and memory resource assignments for a given DNN
and dataflow, while considering hardware constraints. This shows that
the problem is not only how to evaluate an accelerator configuration,
but also how to efficiently navigate the large number of possible
configurations.

\subsection{Automatic Accelerator Generation}
 Other solutions aim at directly automating the complete
 generation of DNN accelerators. Works such as AutoDNNChip
 \cite{autodnnchip} and AutoAI2C\cite{autoai2c}. AutoDNNChip
 introduces a framework composed of a Chip Predictor and a Chip
 Builder, capable of exploring accelerator configurations and
 generating synthesizable hardware implementations based on a given
 DNN model and platform constraints.

\subsection{Limitations of Existing Approaches}
Despite these advances, exploring the full design space of DNN
accelerators remains challenging due to the tight coupling between
architectural decisions and low-level hardware implementation details
such as IP selection and pipeline configuration.

In many existing frameworks, the evaluation models and optimization
objectives are tightly tied to the tool implementation. As a result,
adapting these tools to new optimization criteria, hardware
technologies or cost models could require significant changes to the
internal code base.

\subsection{Hypothesis}

The hypothesis of this thesis is that a hierarchical exploration flow
can make DNN accelerator
design more manageable by separating technology-independent
architectural exploration
from technology-dependent hardware IP instantiation,
while still being modular to allow custom estimations on both levels.

\subsection{Objective \& milestones}
\todo[inline]{Checkear with Jordi \& Ricardo, mis notas dicen que un objetivo
y varias milestones, pero ahora con este comment a lo mejor
puedo haberlo apuntado mal}
\todo[inline]{23_aug. Actualizado. Tmb en project_dev está conectado
en el sentido de como hemos llegado o intentado llegar.}
The objective of this thesis is to assess the proposed hypothesis
through the design and implementation of a tool based on
a hierarchical design space exploration framework for DNN
accelerators.

The %{hypothesis is examined through the}
development process %{of a tool that}
separates high-level architectural exploration from the selection
and evaluation of hardware IP implementations, allowing both stages
to be
explored in a structured and extensible way.


Therefore, the contribution of the prototype is not only the
implementation of a tool, but also
the validation of a structured methodology for separating
architectural
and hardware-oriented design decisions.

To achieve this objective, a set of milestones was established to guide the development and validation of the proposed framework.

\begin{itemize}
    \item Define a hierarchical exploration methodology that
    decouples high-level architectural exploration from low-level
    hardware instantiation.

    \item Develop a mechanism to automatically generate and evaluate
    candidate accelerator architectures using an existing DSE engine.

    \item Define a hardware characterization representation that
    captures the information required for PPA evaluation while
    preserving a high level of abstraction.

    \item Design the framework around modular components and
    pluggable evaluation functions, allowing optimization criteria
    and hardware models to be adapted or extended.

    \item Validate the proposed methodology and implementation using
    representative DNN workloads and characterized hardware
    implementations.
\end{itemize}


\subsection{Proposed approach}

Talos is proposed as a two-level hierarchical design space exploration
framework for DNN accelerators. The exploration starts from a DNN
workload, provided as an ONNX model, together with the optimization
objectives, design constraints, and a characterized hardware IP pool.

The first level explores abstract accelerator architectures using
ZigZag, considering parameters such as the PE array, memory hierarchy,
and bandwidth. The most promising candidates are then passed to a
second level, where their abstract components are matched with
compatible hardware IPs and evaluated using workload-aware PPA
metrics.

The output is a set of feasible accelerator candidates with concrete
IP selections and comparable energy, performance, power, and area
results.

%{It separates the exploration of accelerator architectures from the
%instantiation of specific hardware IPs.}
In the first phase, Talos identifies a set of promising
abstract architectures, cutting down a large part of the design
space. The second level,
the selected architectures are re-evaluated using a high-level
characterization
model of hardware IP blocks to estimate PPA.
Talos is inherently designed modular and open source, so anyone that
is free to add their own evaluation
expressions to support their specific optimization goals.

\subsection{Document Organization}
The rest of this document is organized as follows. The State of the
Art reviews existing DNN accelerator architectures, design space
exploration frameworks, and automatic accelerator generation
approaches. The Methodology section describes the development process
followed during the thesis, including the requirements, main design
decisions, implementation stages, and iterative validation of the
proposed approach.

The Project Development section presents the design and implementation
of Talos in detail. It describes the abstract architecture
representation, the Level 1 architecture exploration, the hardware IP
pool, the Level 2 IP-based evaluation, and the interfaces used to
define optimization objectives. It also discusses the main challenges
and design decisions encountered during development.

The Results section evaluates the resulting framework. It presents the
hardware IP characterization, the architectures explored at Level 1,
their evaluation using characterized IPs at Level 2, and the effect of
the hierarchical approach on candidate selection and design-space
reduction. Finally, the Conclusions section summarizes the main
contributions, limitations, and possible directions for future
development.
