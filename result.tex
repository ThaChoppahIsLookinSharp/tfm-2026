\clearpage
\section{Results}

This section evaluates the implemented Talos flow using the final
exploration results. The objective of these experiments is not only to
find the minimum value of a single metric, but also to show the type of
accelerator configurations that are obtained when energy, area and
performance are considered together.

The section is organized in three parts. First, a complete INT16
AlexNet exploration is used as an end-to-end proof of concept of the
framework. Then, Talos configurations are compared against published
accelerators using workloads selected to make each comparison as close
as possible. Finally, the architectural estimates produced by Talos are
compared against synthesized implementations of the selected designs.

\subsection{Experimental Setup}

All final experiments use the hierarchical exploration flow described
in the previous chapter. Level~1 explores the abstract architecture
using NSGA-II and ZigZag, while Level~2 selects characterized hardware
IPs and evaluates the resulting physical implementation.

The final comparisons use four workload configurations:

\begin{itemize}
    \item a complete AlexNet network using INT16 operands, used as the
    main proof-of-concept exploration;
    \item the five convolutional layers of AlexNet using INT16 and the
    Eyeriss batch-size-four setup, used for comparison with Eyeriss v1;
    \item MobileNet V1 0.5/128 using INT8 operands, used for comparison
    with the dense Eyeriss v2 result;
    \item VGG-16 using FP16 operands, used for comparison with SAURIA.
\end{itemize}

\subsubsection{Characterized Hardware IP Pools}

The Level~2 exploration does not use generic analytical costs for the
on-chip components. Instead, it selects implementations from
characterized hardware IP pools. Three different compute pools are
used in the experiments, matching the numerical representation of
each workload: INT8, INT16, and FP16.

All IPs were characterized using Cadence Genus after mapping the RTL
to a TSMC 65~nm standard-cell library. The common characterization
conditions were a reference frequency of 200~MHz, a supply voltage of
1.2~V, a temperature of 25~$^\circ$C, and the NCCOM process corner.
Power was obtained using the vectorless characterization flow
described in the previous chapter. For every component, Talos stores
its area, timing information, idle and active power, and, when
applicable, the corresponding operation or memory-access energy.

Table~\ref{tab:characterized_pes} summarizes the processing elements
available to Level~2 for the three numerical formats considered in the
final experiments.

\begin{table*}[t]
\centering
\caption{Characterized processing elements used by the Level~2
exploration.}
\label{tab:characterized_pes}

\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.15}

\resizebox{0.98\textwidth}{!}{%
\begin{tabular}{@{}llrrrrrr@{}}
\hline
\textbf{Precision} &
\textbf{Processing element} &
\makecell{\textbf{Area}\\\textbf{[mm$^2$]}} &
\makecell{\textbf{Idle power}\\\textbf{[mW]}} &
\makecell{\textbf{Active power}\\\textbf{[mW]}} &
\makecell{\textbf{$f_{\max}$}\\\textbf{[MHz]}} &
\makecell{\textbf{MACs /}\\\textbf{cycle}} &
\makecell{\textbf{Dynamic energy}\\\textbf{[pJ/MAC]}} \\
\hline

INT8 &
\texttt{pe\_tms4517\_simple} &
0.001275 &
0.106 &
0.436 &
322.27 &
1 &
1.649 \\

INT8 &
\texttt{pe\_gemmini\_like} &
0.002025 &
0.115 &
0.404 &
253.74 &
1 &
1.449 \\

INT8 &
\texttt{pe\_sauria} &
0.002398 &
0.345 &
0.741 &
287.60 &
1 &
1.978 \\

INT8 &
\texttt{pe\_simple\_mac} &
0.001166 &
0.118 &
0.467 &
268.53 &
1 &
1.744 \\

INT8 &
\texttt{pe\_tiny\_xpu} &
0.001293 &
0.106 &
0.463 &
267.38 &
1 &
1.782 \\

INT8 &
\texttt{pe\_alibaba\_int8} &
0.002676 &
0.529 &
1.153 &
346.86 &
1 &
3.120 \\

INT8 &
\texttt{pe\_openeye\_composite\_int8} &
0.070829 &
13.893 &
13.979 &
278.55 &
2 &
0.216 \\

\hline

INT16 &
\texttt{pe\_sauria\_int16} &
0.005094 &
0.593 &
1.649 &
208.20 &
1 &
5.281 \\

INT16 &
\texttt{pe\_gemmini\_like\_int16} &
0.005190 &
0.354 &
1.598 &
200.48 &
1 &
6.221 \\

INT16 &
\texttt{pe\_tiny\_xpu\_int16} &
0.003149 &
0.187 &
1.048 &
200.12 &
1 &
4.305 \\

\hline

FP16 &
\texttt{pe\_sauria\_fp16} &
0.005479 &
1.125 &
1.778 &
200.00 &
1 &
3.261 \\

FP16 &
\texttt{pe\_fpnew\_fp16\_fma} &
0.004672 &
1.061 &
1.605 &
200.08 &
1 &
2.723 \\

\hline
\end{tabular}%
}
\end{table*}

The number of alternatives is intentionally different between
precisions. The INT8 pool provides seven compute implementations,
while the INT16 and FP16 pools contain three and two alternatives,
respectively. Level~2 can therefore select between different physical
PE implementations without changing the Level~1 architecture.

One particular case is
\texttt{pe\_openeye\_composite\_int8}. Unlike the other INT8
implementations, it provides two MAC operations per cycle and
represents a larger composite processing element. This explains its
considerably larger area and static power and gives Level~2 a
different area--performance trade-off from the remaining INT8
implementations.

The memory design space is shared by the three pools. It contains
twelve small register-file implementations and three larger
global-buffer implementations. The register files include both 1RW
and 1R1W organizations and cover several capacities and access
bandwidths. The main ranges of the characterized memory components
are summarized in Table~\ref{tab:characterized_memories}.

The INT16 pool is used for both AlexNet experiments, the INT8 pool
for the MobileNet/Eyeriss v2 comparison, and the FP16 pool for the
VGG-16/SAURIA comparison. This keeps the workload numerical
representation and the Level~2 compute implementation consistent
throughout the final evaluation.

\begin{table}[t]
\centering
\caption{Characterized memory design space shared by the INT8,
INT16, and FP16 Level~2 pools.}
\label{tab:characterized_memories}

\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}

\resizebox{0.98\columnwidth}{!}{%
\begin{tabular}{@{}lccccc@{}}
\hline
\textbf{Type} &
\textbf{Count} &
\makecell{\textbf{Capacity}\\\textbf{[bit]}} &
\makecell{\textbf{Bandwidth}\\\textbf{[bit/cycle]}} &
\makecell{\textbf{Area}\\\textbf{[mm$^2$]}} &
\makecell{\textbf{$f_{\max}$}\\\textbf{[MHz]}} \\
\hline

Register file &
12 &
64--2048 &
8--256 &
0.000719--0.021068 &
794--1045 \\

Global buffer &
3 &
8192--131072 &
64--1024 &
0.0856--1.3902 &
591--681 \\

\hline
\end{tabular}%
}
\end{table}

The register-file pool ranges from 64 to 2048 bits and provides
bandwidths between 8 and 256 bits per cycle. The three global-buffer
alternatives correspond to 8192, 32768, and 131072 bits, with
bandwidths of 64, 256, and 1024 bits per cycle, respectively.
Level~2 does not necessarily consider all of these implementations
for every architecture. Capacity and bandwidth requirements inherited
from Level~1 are used to discard incompatible memories before the
physical objective is evaluated.

External DRAM is modeled separately from the on-chip accelerator. For
this reason, the tables report both on-chip energy and power and, when
available, the corresponding values including the modeled DRAM
contribution. This distinction is particularly important when comparing
Talos against published chips, since most accelerator papers report
chip power but do not provide the energy of the complete external
memory system.

The Level~2 objectives are denoted as E, A, and P. E minimizes energy
per inference, A minimizes accelerator area, and P favors workload
performance. Their combinations correspond to multi-objective selection
using the scoring method described in the previous chapter.

\subsection{End-to-End INT16 AlexNet Exploration}
\label{sec:results_alexnet_proof}

The complete INT16 AlexNet experiment is used as the main proof of
concept of the tool. Unlike the Eyeriss-specific experiment presented
later, this workload contains the complete AlexNet topology used by
Talos, including the convolutional and fully connected layers. The
objective here is therefore not to reproduce a published accelerator,
but to show the different physical designs obtained by the complete
Talos exploration flow.

Table~\ref{tab:alexnet_int16_talos} shows the selected implementation
for each Level~2 objective combination.

\begin{table*}[t]
\centering
\caption{Talos design configurations obtained for the complete INT16
AlexNet workload under different Level~2 optimization objectives.}
\label{tab:alexnet_int16_talos}

\setlength{\tabcolsep}{3.2pt}
\renewcommand{\arraystretch}{1.15}

\resizebox{0.99\textwidth}{!}{%
\begin{tabular}{@{}lcccccccc@{}}
\hline
\textbf{Configuration} &
\textbf{Technology} &
\textbf{Precision} &
\makecell{\textbf{On-chip Energy}\\\textbf{(mJ/inf.)$^{a}$}} &
\makecell{\textbf{Energy incl. DRAM}\\\textbf{(mJ/inf.)$^{b}$}} &
\textbf{Area} &
\makecell{\textbf{On-chip}\\\textbf{Power$^{a}$}} &
\makecell{\textbf{Total}\\\textbf{Power$^{b}$}} &
\makecell{\textbf{Latency}\\\textbf{(cycles)$^{c}$}} \\
\hline

Talos (E) &
65\,nm CMOS &
INT16 &
39.757 &
112.869 &
5.111\,mm$^2$ &
1.256\,W &
3.565\,W &
6332862 \\

Talos (E+A) &
65\,nm CMOS &
INT16 &
88.076 &
244.724 &
0.512\,mm$^2$ &
0.109\,W &
0.302\,W &
162103247 \\

Talos (A) &
65\,nm CMOS &
INT16 &
55.723 &
450.138 &
0.202\,mm$^2$ &
0.045\,W &
0.363\,W &
248021034 \\

Talos (P) &
65\,nm CMOS &
INT16 &
4650.562 &
4708.420 &
1493.550\,mm$^2$ &
280.541\,W &
284.031\,W &
3315424 \\

Talos (A+P) &
65\,nm CMOS &
INT16 &
51.819 &
198.911 &
1.701\,mm$^2$ &
0.465\,W &
1.786\,W &
22272482 \\

Talos (E+P) &
65\,nm CMOS &
INT16 &
45.035 &
124.780 &
8.832\,mm$^2$ &
2.285\,W &
6.331\,W &
3942051 \\

Talos (E+A+P) &
65\,nm CMOS &
INT16 &
51.142 &
198.234 &
1.701\,mm$^2$ &
0.459\,W &
1.780\,W &
22272482 \\

\hline
\end{tabular}%
}

\vspace{2mm}
\begin{minipage}{0.99\textwidth}
\footnotesize

$^{a}$ On-chip energy includes the selected processing elements and
on-chip memory hierarchy, excluding the modeled external DRAM
contribution. On-chip power is computed over the complete workload
execution.

$^{b}$ Total energy and power include the modeled external DRAM
contribution. The total workload energy is the quantity used by Talos
when energy is included as an optimization objective.

$^{c}$ Latency is expressed in workload cycles at the 200\,MHz
operating frequency used for the Talos characterization.

The workload corresponds to the complete AlexNet topology using
16-bit integer operands. INT16 is the numerical representation selected
for this Talos experiment and is not part of the original AlexNet
specification.

\end{minipage}
\end{table*}

The results show the expected conflict between the three objectives.
The energy-only configuration obtains the lowest total energy,
112.869~mJ per inference, but requires 5.111~mm$^2$. At the opposite
extreme, the area-only configuration reduces the accelerator to only
0.202~mm$^2$, but its latency increases to 248021034 cycles and the
large execution time also increases the total energy to
450.138~mJ per inference.

The performance-only result shows why a performance objective should
not normally be used without an area or energy constraint. Talos keeps
increasing the available parallel hardware until it obtains the lowest
latency in the explored set. The resulting implementation reaches
3315424 cycles, but requires 1493.550~mm$^2$ and more than 280~W of
on-chip power. This point is not a realistic accelerator target, but it
is useful to verify that the exploration is correctly exposing the
trade-off instead of hiding it behind a fixed architecture template.

The combined objectives produce more useful designs. In particular,
the E+A+P configuration uses 1.701~mm$^2$, consumes 51.142~mJ of
on-chip energy per inference, and completes the workload in 22272482
cycles. It is not the best point in any single metric, but it avoids the
very large area of the performance-oriented configurations and the very
large latency of the area-oriented configurations. The A+P and E+A+P
solutions also converge to the same area and latency, while including
energy in the objective slightly improves both the on-chip and total
energy. This configuration is therefore used as the representative
balanced design in the later synthesis validation.

Another result visible in Table~\ref{tab:alexnet_int16_talos} is the
importance of the external memory contribution. For the E+A+P design,
the on-chip energy is 51.142~mJ per inference, while the complete model
including DRAM reaches 198.234~mJ. The difference is even larger for
some of the area-oriented points. This is why the following comparisons
keep on-chip and total figures separated whenever the published
accelerator does not report DRAM energy.

\subsection{Comparison with Published Accelerators}

The next experiments compare Talos against three published DNN
accelerators. A different workload is used for each comparison in order
to reproduce the network, precision, and operating conditions reported
by the corresponding paper as closely as possible.

These comparisons should still be interpreted as reference points
rather than exact chip-to-chip benchmarks. Talos uses characterized RTL
blocks and analytical workload activity, while the published results
come from fabricated accelerators or post-layout implementations.
Voltage, memory system assumptions, physical design effort, and the
exact definition of reported power can also differ between works.

\subsubsection{Eyeriss v1: AlexNet INT16}

Eyeriss v1 provides the closest comparison for the INT16 flow because
both designs use a 65~nm technology, 16-bit integer arithmetic and a
200~MHz core frequency. For this experiment, the Talos workload is not
the complete AlexNet model from the previous subsection. Instead, it
reproduces the five AlexNet convolutional layers and the batch-size-four
setup used by Eyeriss. Talos energy and latency are normalized per
image.

\begin{table*}[t]
\centering
\caption{Comparison of the Talos INT16 AlexNet convolutional
configurations selected under different optimization objectives and
the Eyeriss v1 accelerator.}
\label{tab:eyeriss_v1_comparison}

\setlength{\tabcolsep}{2.8pt}
\renewcommand{\arraystretch}{1.15}

\resizebox{0.99\textwidth}{!}{%
\begin{tabular}{@{}lccccccccc@{}}
\hline
\textbf{Configuration} &
\textbf{Technology} &
\textbf{Precision} &
\makecell{\textbf{On-chip Energy}\\\textbf{(mJ/inf.)$^{a}$}} &
\makecell{\textbf{Energy incl. DRAM}\\\textbf{(mJ/inf.)$^{b}$}} &
\textbf{Area} &
\makecell{\textbf{On-chip}\\\textbf{Power$^{a}$}} &
\makecell{\textbf{Total}\\\textbf{Power$^{b}$}} &
\makecell{\textbf{Latency}\\\textbf{(Mcycles/inf.)}} &
\textbf{Performance} \\
\hline

Talos (E) &
65\,nm CMOS &
INT16 &
44.060 &
57.059 &
3.251\,mm$^2$ &
0.728\,W &
0.943\,W &
12.108 &
16.52 inf/s \\

Talos (E+A) &
65\,nm CMOS &
INT16 &
56.486 &
71.998 &
2.828\,mm$^2$ &
0.616\,W &
0.785\,W &
18.337 &
10.91 inf/s \\

Talos (A) &
65\,nm CMOS &
INT16 &
45.536 &
214.718 &
0.265\,mm$^2$ &
0.058\,W &
0.275\,W &
155.908 &
1.28 inf/s \\

Talos (P) &
65\,nm CMOS &
INT16 &
550.904 &
553.165 &
386.255\,mm$^2$ &
74.224\,W &
74.528\,W &
1.484 &
134.73 inf/s \\

Talos (A+P) &
65\,nm CMOS &
INT16 &
44.060 &
57.059 &
3.251\,mm$^2$ &
0.728\,W &
0.943\,W &
12.108 &
16.52 inf/s \\

Talos (E+P) &
65\,nm CMOS &
INT16 &
78.835 &
87.571 &
46.633\,mm$^2$ &
10.442\,W &
11.599\,W &
1.510 &
132.45 inf/s \\

Talos (E+A+P) &
65\,nm CMOS &
INT16 &
44.060 &
57.059 &
3.251\,mm$^2$ &
0.728\,W &
0.943\,W &
12.108 &
16.52 inf/s \\

\hline

Eyeriss v1 \cite{chen2017eyeriss} &
65\,nm CMOS &
INT16 &
8.01$^{c}$ &
--$^{d}$ &
12.25\,mm$^2$ &
0.278\,W$^{c}$ &
--$^{d}$ &
$\sim$5.76$^{e}$ &
34.7 inf/s$^{e}$ \\

\hline
\end{tabular}%
}

\vspace{1mm}
\begin{minipage}{0.97\textwidth}
\footnotesize

$^{a}$ Talos on-chip energy excludes the external DRAM contribution.
The corresponding on-chip power is computed over the complete workload
execution. The Talos AlexNet convolutional workload uses batch size 4;
energy and latency are normalized per image.

$^{b}$ Talos total energy and power include the modeled external DRAM
contribution. The total workload energy is the quantity used by Talos
when energy is included as an optimization objective.

$^{c}$ Eyeriss v1 reports a measured chip power of 278\,mW and a
throughput of 34.7 frames/s for the five convolutional layers of
AlexNet at a batch size of 4. Its energy per image is therefore derived
as $E_{\mathrm{inf}}=P/(\mathrm{inf/s})\simeq8.01$\,mJ/inf.

$^{d}$ Eyeriss v1 reports external DRAM traffic, but not the
corresponding external-memory energy or total system power. These
quantities are therefore left unspecified.

$^{e}$ Eyeriss v1 operates at a 200\,MHz core frequency. Its equivalent
latency is derived from the reported throughput as
$N_{\mathrm{cycles}}=200\times10^6/34.7\simeq5.76$ million cycles per
image. Talos latency is reported in equivalent million cycles per image
at the same 200\,MHz reference frequency.

\end{minipage}
\end{table*}

For this workload, the balanced E+A+P objective selects the same Talos
implementation as the E and A+P cases. The design occupies
3.251~mm$^2$, which is considerably smaller than the 12.25~mm$^2$
reported for Eyeriss. However, the energy and performance results are
worse. Talos requires 44.060~mJ of on-chip energy per image and
12107646 cycles, compared with 8.01~mJ and approximately 5763689
cycles for Eyeriss.

This result is useful for two reasons. First, it shows that reducing
area alone does not mean that the generated design is competitive in
energy or latency. Second, it provides a realistic reference for the
current Talos cost model: the generated architecture is much smaller,
but the custom Eyeriss implementation is clearly more efficient for the
specific row-stationary AlexNet workload for which it was designed.
The comparison is also not completely symmetric because Eyeriss power
is measured at 1~V, while the Talos IP characterization uses 1.2~V.

As in the complete AlexNet experiment, optimizing only performance
produces an extreme design. The P configuration reduces latency to
1484444 cycles, but does so with an area of 386.255~mm$^2$ and more
than 74~W of on-chip power. The E+P result reaches a latency close to
this point with a much smaller, although still large, 46.633~mm$^2$
implementation. This again shows the need to consider area together
with performance during the physical selection stage.

\subsubsection{Eyeriss v2: MobileNet V1 INT8}

The Eyeriss v2 comparison uses the dense MobileNet V1 0.5/128 result.
This is a particularly useful reference because both Talos and Eyeriss
v2 use a 65~nm technology, INT8 arithmetic and a 200~MHz operating
frequency. The dense Eyeriss result is used instead of the sparse mode
because the Talos workload does not model the sparsity exploited by the
sparse Eyeriss v2 configuration.

\begin{table*}[t]
\centering
\caption{Comparison of the Talos INT8 MobileNet V1 0.5/128
configurations selected under different optimization objectives and
the dense Eyeriss v2 accelerator.}
\label{tab:eyerissv2_mobilenet_comparison}

\setlength{\tabcolsep}{2.8pt}
\renewcommand{\arraystretch}{1.15}

\resizebox{0.99\textwidth}{!}{%
\begin{tabular}{@{}lccccccccc@{}}
\hline
\textbf{Configuration} &
\textbf{Technology} &
\textbf{Precision} &
\makecell{\textbf{On-chip Energy}\\\textbf{(mJ/inf.)$^{a}$}} &
\makecell{\textbf{Energy incl. DRAM}\\\textbf{(mJ/inf.)$^{b}$}} &
\textbf{Area} &
\makecell{\textbf{On-chip}\\\textbf{Power$^{a}$}} &
\makecell{\textbf{Total}\\\textbf{Power$^{b}$}} &
\makecell{\textbf{Latency}\\\textbf{(Mcycles/inf.)}} &
\textbf{Performance} \\
\hline

Talos (E) &
65\,nm CMOS &
INT8 &
0.972 &
2.795 &
5.672\,mm$^2$ &
1.199\,W &
3.450\,W &
0.162 &
1234.37 inf/s \\

Talos (E+A) &
65\,nm CMOS &
INT8 &
2.881 &
7.276 &
0.441\,mm$^2$ &
0.090\,W &
0.227\,W &
6.417 &
31.17 inf/s \\

Talos (A) &
65\,nm CMOS &
INT8 &
1.889 &
17.320 &
0.170\,mm$^2$ &
0.041\,W &
0.373\,W &
9.285 &
21.54 inf/s \\

Talos (P) &
65\,nm CMOS &
INT8 &
14.094 &
15.524 &
110.515\,mm$^2$ &
24.326\,W &
26.793\,W &
0.116 &
1725.94 inf/s \\

Talos (A+P) &
65\,nm CMOS &
INT8 &
1.399 &
4.828 &
1.536\,mm$^2$ &
0.388\,W &
1.338\,W &
0.721 &
277.23 inf/s \\

Talos (E+P) &
65\,nm CMOS &
INT8 &
2.079 &
3.540 &
15.237\,mm$^2$ &
3.097\,W &
5.275\,W &
0.134 &
1489.99 inf/s \\

Talos (E+A+P) &
65\,nm CMOS &
INT8 &
1.388 &
4.816 &
1.536\,mm$^2$ &
0.385\,W &
1.335\,W &
0.721 &
277.23 inf/s \\

\hline

Eyeriss v2 \cite{chen2019eyerissv2} &
65\,nm TSMC LP &
INT8 &
0.508$^{c}$ &
--$^{d}$ &
\makecell{2695k NAND2\\+ 246\,kB SRAM$^{e}$} &
0.651\,W$^{c}$ &
--$^{d}$ &
$\sim$0.156$^{f}$ &
1282.1 inf/s$^{f}$ \\

\hline
\end{tabular}%
}

\vspace{1mm}
\begin{minipage}{0.97\textwidth}
\footnotesize

$^{a}$ Talos on-chip energy excludes the external DRAM contribution.
The corresponding on-chip power is computed over the complete workload
execution.

$^{b}$ Talos total energy and power include the modeled external DRAM
contribution. The total workload energy is also the quantity used by
the optimization whenever energy is included as an objective.

$^{c}$ For dense MobileNet V1 0.5/128, Eyeriss v2 reports
1969.8\,inf/J and 1282.1\,inf/s. The corresponding on-chip energy and
average chip power are derived as
$E_{\mathrm{inf}}=1/1969.8\simeq0.508$\,mJ/inf. and
$P=(1282.1)/(1969.8)\simeq0.651$\,W.

$^{d}$ Eyeriss v2 reports external DRAM traffic for the MobileNet
workload, but does not provide the corresponding DRAM energy or total
system power.

$^{e}$ Eyeriss v2 reports 2695k NAND-2-equivalent gates for the
logic and 246\,kB of on-chip SRAM. The reported gate count excludes the
SRAM macros. Since the paper does not provide the complete post-layout
silicon area in mm$^2$, the physical cost is shown in its published form
rather than converted to an approximate area.

$^{f}$ Both Talos and Eyeriss v2 use a 200\,MHz operating frequency in
this comparison. Eyeriss v2 equivalent latency is derived from its
reported throughput as
$N_{\mathrm{cycles}}=200\times10^6/1282.1\simeq0.156$ million cycles
per inference. The MobileNet benchmark uses batch size 1, width
multiplier 0.5, and a $128\times128$ input.

\end{minipage}
\end{table*}

The energy-oriented Talos point is the closest configuration to Eyeriss
v2 in terms of performance. It completes the workload in 162026 cycles,
which is very close to the 155994-cycle value derived for Eyeriss v2.
Its on-chip energy, however, is 0.972~mJ per inference compared with
0.508~mJ for Eyeriss v2, and its on-chip power is 1.199~W compared with
0.651~W. Therefore, the current Talos implementation approaches the
published accelerator in latency for this workload, but still pays a
clear energy and power penalty.

The E+P point reduces latency further to 134229 cycles, at the cost of a
15.237~mm$^2$ accelerator and 2.079~mJ of on-chip energy. The
performance-only point is faster again, but its area rises to
110.515~mm$^2$. In contrast, the balanced E+A+P point reduces the area
to 1.536~mm$^2$ and on-chip power to 0.385~W, while still completing an
inference in 721433 cycles. This design is significantly slower than
Eyeriss v2, but represents a much more conservative physical point in
the explored Talos space.

Eyeriss v2 does not provide a complete post-layout area in
mm$^2$ for this implementation. Instead, the paper reports 2695k
NAND-2-equivalent logic gates, excluding SRAM, together with 246\,kB of
on-chip SRAM. Consequently, the physical cost is retained in the
published gate-and-memory form rather than converted to a potentially
misleading silicon-area estimate.

The MobileNet result also shows that the configuration preferred by a
balanced objective does not need to be the configuration that most
closely reproduces the published accelerator. Talos can instead expose
both alternatives: a point with similar latency to Eyeriss v2 and a
smaller balanced point when area is also considered.

\subsubsection{SAURIA: VGG-16 FP16}

SAURIA is used as the floating-point comparison. Both experiments use
VGG-16 with FP16 arithmetic, but the physical technologies are
different: Talos uses the characterized 65~nm pool, while SAURIA is a
post-layout GF22FDX 22~nm implementation. For the VGG-16 exploration,
the final E+A+P solution was selected from the larger exploration pool,
which produced the best balanced point over the complete set of runs.

\begin{table*}[t]
\centering
\caption{Comparison of the Talos FP16 VGG-16 configurations selected
under different optimization objectives and the SAURIA accelerator.}
\label{tab:vgg16_fp16_sauria_comparison}

\setlength{\tabcolsep}{2.8pt}
\renewcommand{\arraystretch}{1.15}

\resizebox{0.99\textwidth}{!}{%
\begin{tabular}{@{}lccccccccc@{}}
\hline
\textbf{Configuration} &
\textbf{Technology} &
\textbf{Precision} &
\makecell{\textbf{On-chip Energy}\\\textbf{(mJ/inf.)$^{a}$}} &
\makecell{\textbf{Energy incl. DRAM}\\\textbf{(mJ/inf.)$^{b}$}} &
\textbf{Area} &
\makecell{\textbf{On-chip}\\\textbf{Power$^{a}$}} &
\makecell{\textbf{Total}\\\textbf{Power$^{b}$}} &
\makecell{\textbf{Latency}\\\textbf{(Mcycles/inf.)}} &
\textbf{Performance} \\
\hline

Talos (E) &
65\,nm CMOS &
FP16 &
763.4 &
1201.6 &
14.450\,mm$^2$ &
3.774\,W &
5.940\,W &
40.46 &
4.94 inf/s \\

Talos (E+A) &
65\,nm CMOS &
FP16 &
1259.4 &
2689.6 &
0.483\,mm$^2$ &
0.114\,W &
0.243\,W &
2209.38 &
0.091 inf/s \\

Talos (A) &
65\,nm CMOS &
FP16 &
1534.2 &
5784.9 &
0.226\,mm$^2$ &
0.058\,W &
0.220\,W &
5249.56 &
0.038 inf/s \\

Talos (P) &
65\,nm CMOS &
FP16 &
4239.2 &
4345.4 &
114.820\,mm$^2$ &
26.203\,W &
26.860\,W &
32.36 &
6.18 inf/s \\

Talos (A+P) &
65\,nm CMOS &
FP16 &
927.8 &
2121.9 &
1.975\,mm$^2$ &
0.518\,W &
1.184\,W &
358.53 &
0.558 inf/s \\

Talos (E+P) &
65\,nm CMOS &
FP16 &
790.5 &
1320.8 &
14.450\,mm$^2$ &
3.961\,W &
6.617\,W &
39.92 &
5.01 inf/s \\

Talos (E+A+P) &
65\,nm CMOS &
FP16 &
762.3 &
1336.5 &
3.641\,mm$^2$ &
0.947\,W &
1.660\,W &
161.01 &
1.24 inf/s \\

\hline

SAURIA \cite{sauria2023} &
\makecell{22\,nm\\GF22FDX} &
FP16 &
$\sim$42.3$^{c}$ &
--$^{d}$ &
0.897\,mm$^2$ &
0.258\,W$^{c}$ &
--$^{d}$ &
$\sim$91.02$^{e}$ &
\makecell{$\sim$6.10 inf/s$^{e}$\\284\,GFLOP/s peak} \\

\hline
\end{tabular}%
}

\vspace{1mm}
\begin{minipage}{0.97\textwidth}
\footnotesize

$^{a}$ Talos on-chip energy excludes the external DRAM contribution,
i.e.,
$E_{\mathrm{on-chip}} =
E_{\mathrm{workload}} - E_{\mathrm{DRAM}}$.
The corresponding on-chip power is computed over the complete
workload execution.

$^{b}$ Total Talos energy and power include the modeled external DRAM
contribution. The total workload energy is also the quantity used by
the optimization when energy is included as an objective.

$^{c}$ The SAURIA VGG-16 on-chip energy is a derived estimate obtained
from the reported accelerator power and VGG-16 execution time:
$E \simeq 0.258\,\mathrm{W}\times0.164\,\mathrm{s}
\simeq42.3\,\mathrm{mJ/inference}$.
The 258\,mW value is reported for operation at peak throughput;
therefore, the derived VGG-16 energy should be interpreted as an
approximate value rather than a directly measured
energy-per-inference result.

$^{d}$ A directly comparable SAURIA total energy or system power
including external DRAM is not reported.

$^{e}$ SAURIA reports a VGG-16 execution time of approximately
164\,ms using the on-the-fly data feeder and assuming 6.4\,GB/s of
external DRAM bandwidth. Using the reported maximum frequency of
555\,MHz gives approximately
$0.164\times555\times10^6 \simeq 91.02$ million cycles per inference,
corresponding to approximately 6.10 inferences/s. The benchmark
considers convolutional and fully-connected layers and excludes
pooling and activation layers.

Talos latency cycles are derived from the reported workload latency
using the 200\,MHz reference frequency employed in the characterization
and Level~2 evaluation.

\end{minipage}
\end{table*}
The VGG-16 exploration presents the same general trade-off observed in
the integer workloads. The area-only point is very small, at
0.226~mm$^2$, but only processes 0.038 inferences/s. The
performance-only point reaches 6.18 inferences/s, which is close to the
VGG-16 rate derived from the SAURIA execution time, but requires
114.820~mm$^2$ and 26.203~W of on-chip power. It is therefore not a
competitive physical implementation even though it reaches a similar
workload-level throughput.

The balanced E+A+P point is more representative of what Talos is
intended to generate. It occupies 3.641~mm$^2$, consumes 0.947~W of
on-chip power and reaches 1.24 inferences/s. SAURIA is still clearly
better in area, power and performance, with 0.897~mm$^2$, 0.258~W and
approximately 6.10 inferences/s. However, this is also the least direct
of the three published-accelerator comparisons because SAURIA uses a
much newer 22~nm technology and a heavily optimized custom FP16
implementation.

The energy-only Talos point reaches 4.94 inferences/s and the E+P point
reaches 5.01 inferences/s, but both require 14.450~mm$^2$ and several
watts of on-chip power. This shows that, for the current characterized
FP16 pool, obtaining a SAURIA-like throughput is possible only by
accepting a much larger physical cost. The result therefore identifies
the FP16 compute implementation as an important target for future
improvement of the available IP pool.

\subsection{Validation Against Synthesized Implementations}

The comparisons above evaluate the quality of the final accelerator
points against published designs. A different question is whether the
physical cost predicted by Talos is representative of the hardware
that is obtained when the selected configuration is actually built.
For this purpose, the E+A+P configurations were instantiated and
synthesized, and their resulting area and active power were compared
against the Talos Level~2 estimates.

Table~\ref{tab:talos_estimation_validation} summarizes the available
validation results. External DRAM is excluded, since the synthesized
implementation only contains the on-chip accelerator.

\begin{table*}[t]
\centering
\caption{Summary of the deviation between Talos architectural
estimates and synthesized E+A+P accelerator configurations.}
\label{tab:talos_estimation_validation}

\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.15}

\resizebox{0.98\textwidth}{!}{%
\begin{tabular}{@{}lrrrrrr@{}}
\hline
\textbf{Configuration} &
\makecell{\textbf{Area}\\\textbf{Talos (mm$^2$)}} &
\makecell{\textbf{Area}\\\textbf{Synth. (mm$^2$)}} &
\makecell{\textbf{Area}\\\textbf{Deviation}} &
\makecell{\textbf{Power}\\\textbf{Talos (W)}} &
\makecell{\textbf{Power}\\\textbf{Synth. (W)}} &
\makecell{\textbf{Power}\\\textbf{Deviation}} \\
\hline

Eyeriss/AlexNet INT16 &
3.251 &
3.332 &
$-2.43\%$ &
0.728 &
0.664 &
$+9.56\%$ \\

SAURIA/VGG-16 FP16 &
3.641 &
2.657 &
$+37.04\%$ &
0.947 &
0.651 &
$+45.46\%$ \\

AlexNet INT16 &
1.701 &
1.729 &
$-1.64\%$ &
0.459 &
0.373 &
$+23.12\%$ \\

Eyeriss v2/MobileNet INT8 &
1.536 &
1.554 &
$-1.18\%$ &
0.385 &
0.333 &
$+15.61\%$ \\

\hline
\end{tabular}%
}

\vspace{1mm}
\begin{minipage}{0.96\textwidth}
\footnotesize
The deviation is computed as
$\Delta=(X_{\mathrm{Talos}}-X_{\mathrm{synth}})/
X_{\mathrm{synth}}\times100$.
Positive values indicate that Talos overestimates the synthesized
result, whereas negative values indicate an underestimation.
Talos power corresponds to the estimated on-chip workload power and
is compared against the active power obtained from synthesis.
External DRAM is excluded from this comparison.
\end{minipage}

\end{table*}

For the two INT16 configurations, the area estimate is very close to
the synthesized result. The Eyeriss-compatible design is underestimated
by 2.43\%, while the complete AlexNet design is underestimated by only
1.64\%. This is a good result for the area model and shows that summing
the characterized physical IPs gives a useful estimate of the final
accelerator footprint for these two cases.

Power is less accurate, but the error remains moderate for the integer
implementations. Talos overestimates the Eyeriss-compatible design by
9.56\% and the complete AlexNet design by 23.12\%. In both cases the
error is conservative: the implementation obtained after synthesis
consumes less active power than predicted by Talos.

The FP16 VGG-16 configuration shows a larger difference. Talos
estimates 3.641~mm$^2$ and 0.947~W, while the synthesized design reports
2.657~mm$^2$ and 0.651~W. The corresponding deviations are 37.04\% in
area and 45.46\% in power. Therefore, the current Level~2 model should
not be interpreted as a replacement for physical implementation. It is
accurate enough to guide the exploration and rank candidate IP
combinations, but its precision depends on the type of IPs being
combined. In the current experiments the integer configurations match
the synthesis results considerably better than the FP16 configuration.

This validation is important for the role of Level~2. The purpose of
the level is to introduce technology-dependent information early enough
to make better architectural decisions, without paying the cost of
synthesizing every candidate in the design space. The results show that
this approximation can be quite close for some configurations while
still leaving enough error that the final selected accelerator should be
validated by a conventional implementation flow.

\subsection{Results Summary}

The final experiments show several consistent behaviors across the
three workloads.

First, the Level~2 objective has a strong effect on the resulting
hardware. Energy-only configurations tend to use more hardware to
reduce memory traffic and execution time, while area-only
configurations accept very long execution times. Performance-only
optimization repeatedly produces very large accelerators with high
power consumption. The multi-objective configurations avoid these
extremes and produce more realistic accelerator candidates.

Second, DRAM cannot be ignored when evaluating the generated designs.
For several Talos points, the total energy including the modeled DRAM is
much larger than the energy of the on-chip accelerator. Published
accelerators often do not report this contribution, so separating both
values is necessary to avoid presenting an unfair comparison.

Third, the comparisons with Eyeriss v1, Eyeriss v2 and SAURIA show
that Talos does not automatically outperform a custom accelerator
optimized for one specific workload. The strongest result is obtained
for the dense MobileNet/Eyeriss v2 case, where the Talos energy-oriented
configuration reaches a very similar latency, although with higher
on-chip energy and power. The Eyeriss v1 comparison shows a much
smaller Talos area but worse energy and latency, while the SAURIA
comparison shows that the current FP16 pool is still far from the
physical efficiency of a custom 22~nm implementation.

Finally, the synthesis validation supports the use of characterized IPs
as a second exploration level. The area of both INT16 configurations is
predicted within 2.5\% of the synthesized result, and their power is
predicted within approximately 10--23\%. The FP16 case has a larger
error, with Talos overestimating both area and power. Therefore, the
current framework is best understood as a design-space reduction and
selection tool: it can identify meaningful accelerator candidates using
physical IP information, after which the final few designs can be
passed to synthesis for accurate implementation results.
